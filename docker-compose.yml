version: '3.8'

services:
  app:
    build: .
    container_name: mi-app
    ports:
      - "4014:3000"
    env_file:
      - .env
    volumes:
      - .:/app   # (opcional en desarrollo)
    restart: always